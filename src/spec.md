# Specification

## Summary
**Goal:** Publish the currently released build live on the custom domain `sciquiztn-knowledgebyte` with no functional or UI changes.

**Planned changes:**
- Deploy the existing released build to production using the exact custom domain string `sciquiztn-knowledgebyte`.
- Verify the live deployment loads successfully from the live URL without deployment errors.

**User-visible outcome:** Users can access and use the app via the live URL on the `sciquiztn-knowledgebyte` domain, with the same functionality and UI as the current released build.
